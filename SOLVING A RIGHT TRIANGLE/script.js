// var a = document. getElementById("myA").value;
// var b = document. getElementById("myB").value;
// var c = document. getElementById("myC").value;
// var 𝒶 = document. getElementById("my𝒶").value;
// var 𝛽 = document. getElementById("my𝛽").value;
//
// // Begin solving for all three side lengths
// const solveForSides = ( ) => {
// // Solve for "c"
//     if (a === " " && b ===" " && c === null) {
//         return (Math.sqrt((a * a) + (b * b)));
//     } else
// // // Solve for b
//     if (a != 0 && c != 0 && b === null) {
//         return (Math.sqrt((c * c) - (a * a)));
//     } else
// // Solve for a
//     if (b != 0 && c != 0 && a === null) {
//         return (Math.sqrt((c * c) - (b * b)));
//     } else
//         return ("Re-enter your side lengths");
// document.getElementById("demo").innerHTML = solveForSides( );
// }
// // const solveForAlpha = ( ) => {
// // // Solve for 𝒶 alpha  Use one of three possible methods:
// // // (sin^-1 (b/c)
// // // (cos^-1 (a/c)
// // // (tan^-1 (b/c)
// //     if (b != 0 && c != 0) {
// //         return (sin.arc (b/c));
// //     } else
// //     if (a != 0 && c !=0) {
// //         return (cos.arc(a/c));
// //     } else
// //     if (b != 0 && c != 0) {
// //         return (tan.arc(b/c));
// //     }
// // document.getElementById("demoA").innerHTML = solveForAlpha( );
// // }
// // const solveForBeta = ( ) => {
// // // Solve for Angle Beta
// // // Use one of three methods:
// // // (sin^-1(a/c))
// // // (cos^-1(b/c))
// // // (tan^-1(a/b))
// //     if (a != 0 && c != 0) {
// //         return (Math.asin(a/c));
// //     } else
// //     if (b != 0 && c != 0) {
// //         return (Math.acos(b/c));
// //     } else
// //     if (a != 0 && b != 0) {
// //         return (Math.atan(a/b));
// //     }
// // document.getElementById("demoB").innerHTML = solveForBeta( );
// // }
